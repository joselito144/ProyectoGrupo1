import { MaskSinglenumberDirective } from './mask-singlenumber.directive';

describe('MaskSinglenumberDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskSinglenumberDirective();
    expect(directive).toBeTruthy();
  });
});
